version: '3.8'

services:
  ahey:
    build: .
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    container_name: ahey-app
    restart: unless-stopped
    ports:
      - "824:824"
